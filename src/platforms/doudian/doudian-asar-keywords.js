const {
  SEARCH_KEYWORDS,
  PRIORITY_URLS,
  ENTRY_CANDIDATES,
  CONFIG_FILES,
  SENSITIVE_LOG_PATTERNS,
} = require('./doudian-asar-keywords-data');

const {
  PATCH_MARKER,
  PRELOAD_TEST_FLAG,
  PATCH_TARGET_FILES,
  PATCH_TARGET_FILE,
  WORKSPACE_URL_PATTERN,
  PROTECTED_INSTALL_DIR_PATTERN,
  RECOMMENDED_TEST_DIR,
} = require('./doudian-asar-patch-constants');

module.exports = {
  SEARCH_KEYWORDS,
  PRIORITY_URLS,
  ENTRY_CANDIDATES,
  CONFIG_FILES,
  SENSITIVE_LOG_PATTERNS,
  PATCH_MARKER,
  PRELOAD_TEST_FLAG,
  PATCH_TARGET_FILES,
  PATCH_TARGET_FILE,
  WORKSPACE_URL_PATTERN,
  PROTECTED_INSTALL_DIR_PATTERN,
  RECOMMENDED_TEST_DIR,
};
